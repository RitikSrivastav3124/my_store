name: my_store
description: "Digital credit ledger mobile app for owners and customers."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^0.68.0
  firebase_core: ^3.3.0
  firebase_messaging: ^15.0.4
  flutter_secure_storage: ^9.2.2
  http: ^1.2.2
  intl: ^0.19.0
  path_provider: ^2.1.4
  provider: ^6.1.2
  share_plus: ^10.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
